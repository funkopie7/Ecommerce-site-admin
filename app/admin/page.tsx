import Link from "next/link";
const links = [["Products", "Create and edit catalog entries, prices, costs, images, and visibility."], ["Categories", "Organize catalog collections and product discoverability."], ["Inventory", "Record stock adjustments with a clear reason and audit trail."], ["Orders", "Move orders from confirmed to delivered with carrier and tracking details."]];
export default function AdminPage() { return <main style={{ padding: "56px", fontFamily: "Arial, sans-serif", maxWidth: 900, margin: "auto" }}><p style={{ color: "#64705e", fontSize: 12, letterSpacing: 1 }}>MERCURY / ADMINISTRATION</p><h1 style={{ fontFamily: "Georgia, serif", fontSize: 48 }}>Operations workspace</h1><p style={{ color: "#596057", maxWidth: 600 }}>Use the secured admin API to manage every part of your catalog and fulfilment flow. Supply the <code>x-admin-key</code> header configured by <code>ADMIN_API_KEY</code>.</p><section style={{ display: "grid", gridTemplateColumns: "repeat(2, 1fr)", gap: 16, marginTop: 38 }}>{links.map(([title, description]) => <article key={title} style={{ border: "1px solid #dde0d9", padding: 24, borderRadius: 10 }}><h2>{title}</h2><p>{description}</p><Link href="/">Open dashboard →</Link></article>)}</section></main> }
