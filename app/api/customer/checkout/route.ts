import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";
import { customerFromRequest } from "@/lib/auth";
import { error } from "@/lib/api";
import { prisma } from "@/lib/prisma";
import { totalForItems } from "@/lib/checkout";
const input = z.object({ addressId: z.string(), paymentMethod: z.enum(["COD", "DUMMY_CARD"]) });
export async function POST(request: NextRequest) { const session = await customerFromRequest(request); if (!session) return error("Sign in required", 401); const parsed = input.safeParse(await request.json()); if (!parsed.success) return error("Choose a delivery address and payment method", 400); try { const order = await prisma.$transaction(async (tx) => { const address = await tx.address.findFirst({ where: { id: parsed.data.addressId, customerId: session.customerId } }); if (!address) throw new Error("ADDRESS"); const cart = await tx.cart.findUnique({ where: { customerId: session.customerId }, include: { items: { include: { product: true } } } }); if (!cart?.items.length) throw new Error("CART"); for (const item of cart.items) if (!item.product.visible || item.product.stockQuantity < item.quantity) throw new Error("STOCK"); const subtotal = totalForItems(cart.items.map((item) => ({ unitPrice: item.product.price, quantity: item.quantity }))); const order = await tx.order.create({ data: { number: `MC-${Date.now().toString().slice(-8)}`, customerId: session.customerId, subtotal, total: subtotal, paymentStatus: "SIMULATED_PAID", status: "CONFIRMED", addressSnapshot: address, items: { create: cart.items.map((item) => ({ productId: item.productId, name: item.product.name, sku: item.product.sku, unitPrice: item.product.price, quantity: item.quantity })) } } }); for (const item of cart.items) await tx.product.update({ where: { id: item.productId }, data: { stockQuantity: { decrement: item.quantity } } }); await tx.cartItem.deleteMany({ where: { cartId: cart.id } }); return order; }); return NextResponse.json(order, { status: 201 }); } catch (caught) { const code = caught instanceof Error ? caught.message : ""; return error(code === "STOCK" ? "One or more items are no longer in stock" : code === "CART" ? "Your bag is empty" : "Delivery address not found", code === "STOCK" ? 409 : 400); } }
